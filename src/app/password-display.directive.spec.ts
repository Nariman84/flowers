import { PasswordDisplayDirective } from './password-display.directive';

describe('PasswordDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
